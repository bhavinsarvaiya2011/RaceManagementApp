<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="RaceManagementApp.UI.Views.ActivityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RaceManagementApp.UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBlock Text="Activity" Style="{StaticResource TitleTextBlockStyle}"
          Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="10,10,10,10"/>
        
        

        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="100">
            <TextBlock Text="" FontSize="24" FontWeight="Bold" TextAlignment="Center" Margin="50"/>
            <Button Content="Pre-Stage" Command="{Binding PreStageCommand}" Width="200" Height="50"/>
            <Button Content="Stage" Command="{Binding StageCommand}" Width="200" Height="50"/>
            <Button Content="Green Light" Command="{Binding GreenLightCommand}" Width="200" Height="50"/>
            <Button Content="Red Light" Command="{Binding RedLightCommand}" Width="200" Height="50"/>
            <Button Content="Sensor" Command="{Binding SensorCommand}" Width="200" Height="50"/>
    </StackPanel>
   </Grid>
</Page>
